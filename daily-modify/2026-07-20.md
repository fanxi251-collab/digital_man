# 2026-07-20 修改记录

## 00:30 游客美食推荐与反馈闭环

Task: 新增 `/visitor/food` 与 `/visitor/feedback` 两个一级游客页面，以及独立关系数据、地图联动和管理端处理闭环。

| File | Change | Reason |
| --- | --- | --- |
| `src/lingjing_ai/services/food_store.py`、`api/food_routes.py` | 新增独立美食数据库、筛选、发布校验、图集和游客/管理 API | 让编辑精选内容可独立维护，不耦合景点、RAG 或会话数据 |
| `src/lingjing_ai/services/feedback_store.py`、`api/feedback_routes.py` | 新增匿名反馈、请求幂等、本人进度、联系方式隔离和管理回复状态流转 | 在不新增登录和 IP 记录的前提下形成游客到管理端的处理闭环 |
| `src/lingjing_ai/assets/foods/` | 保存 6 张 Pexels 网络来源 WebP 氛围图和来源/授权记录 | 使用可追溯本地图片避免运行时依赖外站，并明确不冒充餐厅实拍 |
| `frontend/src/views/FoodView.vue`、`FoodDetailDrawer.vue` | 新增即时筛选、精选卡片、详情抽屉、地图定位和询问导游入口 | 延续墨绿、暖金和米白玻璃风格，同时让推荐可直接转入行程决策 |
| `frontend/src/views/FeedbackView.vue` | 新增可访问星级、分类、内容、可选联系方式、提交结果和本人处理进度 | 失败时保留输入，成功时展示编号，并避免暴露其他游客信息 |
| `frontend/src/App.vue`、`VisitorRouteTransition.vue` | 扩展为五项导航并缓存五页，应用湖光溶解过渡和减少动态效果降级 | 让页面切换连贯，同时保留各页状态和滚动位置 |
| `frontend/src/views/MapView.vue`、`useInteractiveMap.js`、`mapPlaces.js` | 合并景点与美食地点、增加暖金图层标记、直达参数和跨类型路线 | 复用现有高德能力，不修改路线后端接口 |
| `frontend/admin_foods.html`、`admin_feedback.html` 及对应静态资源 | 新增美食 CRUD/图片管理和反馈筛选/回复页面，并统一管理端五项导航 | 支持比赛演示环境中的完整内容维护与服务处理流程 |
| `tests/test_food_store.py`、`test_feedback_store.py`、`test_food_feedback_api.py`、前端契约与单元测试 | 覆盖发布规则、筛选、隐私、幂等、导航、地图和页面契约 | 防止新能力及现有游客端功能回归 |

Notes:
- 图片来源改为用户确认的网络下载方案；所有图片转换为 1440×1080 WebP，来源页与 Pexels License 写入 `FOOD_IMAGE_CREDITS.md`。
- Live HTTP 检查：美食、反馈和地图页面均返回 200；空库自动播种 6 条已发布美食，6 条均有可访问封面。
- Verification：`npm test` → 38 passed；新增模块相关 pytest → 42 passed；`python -m pytest -q` → 302 passed（20 条既有 openpyxl 弃用警告）；`npm run build` → 成功，218 个模块完成转换。
- 内置浏览器因先前服务未启动产生的受限错误页而拒绝后续导航，因此本轮未完成三档视口的最终截图验收；未绕过浏览器安全策略，也未调用真实 AI、语音或付费路线服务。

## 07:22 反馈进度短暂断线恢复

Task: 修复游客反馈页首次读取“我的反馈进度”时直接显示 `Failed to fetch` 的问题。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/src/lib/fetchWithNetworkRetry.js` | 新增仅针对无 HTTP 响应网络异常的一次延迟重试 | 后端或 Vite 代理刚启动、重载时可自动跨过短暂断线，不重复成功请求 |
| `frontend/src/views/FeedbackView.vue` | 反馈历史改用网络重试，并把底层英文异常替换为可操作的中文提示 | 避免游客看到实现细节，并保留“刷新进度”手动恢复入口 |
| `frontend/tests/visitor-services.test.js`、`tests/test_frontend.py` | 新增首次失败、第二次成功的回归测试和页面契约 | 证明修复覆盖真实故障模式并防止原始错误提示回归 |

Notes:
- 根因检查：反馈页面、反馈 API 和美食 API 均返回 200；真实浏览器当前正常显示“你还没有提交过反馈”，无控制台错误。问题属于页面加载时服务或开发代理的短暂不可达。
- TDD：回归测试先因重试模块缺失而失败，实现后转绿。
- Verification：`npm test` → 39 passed；`python -m pytest tests/test_frontend.py tests/test_food_feedback_api.py -q` → 30 passed；`npm run build` → 成功，222 个模块完成转换。

## 09:50 管理端导航冲突与越界修复

Task: 修复管理端左侧导航重复、冲突标记外露和激活框越过侧栏边界的问题。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/admin_analytics.html`、`admin_attractions.html`、`admin_documents.html` | 解决正在进行的 Git 合并冲突，只保留统一的五项管理导航和当前页面激活项 | 两套导航与冲突哈希被浏览器同时渲染，直接造成重复内容和异常宽度 |
| `frontend/static/admin.css` | 为侧栏、导航容器和导航项增加 `min-width`、宽度和溢出边界，移动端恢复横向滚动项宽度 | 防止不可断文本或未来内容再次撑破 238px 侧栏 |
| `tests/test_admin_frontend.py` | 断言五页均只有五个导航项、一个激活项、无冲突标记，并验证防溢出 CSS 契约 | 防止未解决冲突或盒模型回归再次进入管理端页面 |

Notes:
- TDD：新契约先分别以导航项 8 个和缺少防溢出规则失败，修复后转绿。
- Verification：`python -m pytest tests/test_admin_frontend.py -q` → 8 passed；`git diff --check` → 通过。
- 浏览器验收：1280px 视口下侧栏宽 238px，所有导航项位于横坐标 20–218px；页面 `scrollWidth` 与视口同为 1280px，冲突文本不存在。

## 10:04 管理页旧导航缓存修复

Task: 修复进入“景点资料”等管理栏目时，浏览器仍可能显示冲突解决前重复导航的问题。

| File | Change | Reason |
| --- | --- | --- |
| `src/lingjing_ai/api/app.py` | 五个管理端 HTML 页面统一返回 `Cache-Control: no-store` | 管理页此前带 ETag 但没有缓存控制，浏览器可能复用包含冲突标记和重复导航的旧 HTML |
| `tests/test_admin_frontend.py` | 新增五个管理页禁用 HTML 缓存的响应头回归测试 | 确保后续进入任一管理栏目时都读取当前页面结构 |

Notes:
- 根因验证：磁盘文件与实时 `/admin/attractions` 响应均为 5 个导航项且无冲突标记，但修复前响应缺少 `Cache-Control`。
- TDD：新增测试先因缺少响应头失败，实现后转绿。
- Verification：`python -m pytest tests/test_admin_frontend.py -q` → 9 passed；实时响应为 `Cache-Control: no-store`；浏览器 1280px 验收无重复、无横向溢出。

## 10:11 完成管理端冲突合并

Task: 确认全部管理端冲突已经解决，并完成当前 `main` 分支的 Git 合并提交。

| File | Change | Reason |
| --- | --- | --- |
| Git merge commit `59c166b` | 合并远端 `main`，保留已解决的管理端五项导航、防越界样式和禁用旧 HTML 缓存修复 | 将已验证的冲突解决结果正式纳入本地分支，并结束未完成的合并状态 |

Notes:
- 合并后 `MERGE_HEAD` 已清除，未解决冲突数为 0；本地 `main` 相对 `origin/main` ahead 5。
- Verification：`python -m pytest -q` → 304 passed；`npm test` → 39 passed；`npm run build` → 成功，222 个模块完成转换。
- 反馈页网络重试、其测试、每日记录和临时测试目录仍保持未暂存，未纳入该合并提交。

## 10:31 数字人回答格式符修复

Task: 修复数字人回答面板直接显示 Markdown 标题符号 `#` 的问题。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/src/components/AssistantAnswer.vue` | 将标题识别从仅支持 `###` 扩展为支持 `#` 至 `######` | AI 返回不同级别 Markdown 标题时统一移除格式符并渲染为标题 |
| `frontend/src/features/digital-human/components/DigitalHumanAnswerPanel.vue` | 复用普通模式的 `AssistantAnswer` 结构化渲染，并适配数字人面板主题样式 | 根因是数字人面板直接以纯文本 `<p>` 输出回答，绕过了已有解析组件 |
| `tests/test_frontend.py` | 增加数字人复用回答组件及多级标题解析契约 | 防止数字人模式再次退回原始 Markdown 文本展示 |

Notes:
- TDD：回归契约先因数字人面板未引用 `AssistantAnswer` 失败，实现后转绿。
- Verification：`python -m pytest tests/test_frontend.py -q` → 27 passed；`npm test` → 39 passed；`npm run build` → 成功，222 个模块完成转换。

## 11:40 项目品牌统一为“灵境智导”

Task: 将当前游客端、管理端、API 标题及使用文档中的项目名称统一为“灵境智导”，并保留内部兼容标识。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/src/App.vue`、`ChatMain.vue`、`ScenicIntro.vue`、`index.html`、`visitor.html` | 替换共享品牌、导游品牌、开场品牌、输入提示和浏览器标题 | 让游客端所有当前入口使用统一中文项目名称 |
| `frontend/admin_*.html` | 五个管理页面统一显示“灵境智导”，管理眉题改为中文组合 | 消除管理端残留的旧英文品牌，同时保留 `LJ` 标记和导航结构 |
| `src/lingjing_ai/api/app.py`、`__init__.py`、`pyproject.toml` | 更新 FastAPI 标题、构建提示标题和人类可读包描述 | 对外元数据使用新品牌，但保留 `lingjing_ai` 与 `lingjing-ai` 技术标识 |
| `evaluation/`、`scripts/`、`src/lingjing_ai/evaluation/reporter.py` | 更新评测集、报告和生成源标题 | 防止重新生成评测资料时恢复旧品牌 |
| `docs/项目部署运行配置说明书.md`、`requirement.md`、`project_structure.md` | 更新当前使用文档的人类可读项目名称 | 统一项目交付材料，同时保留部署路径与命令中的技术标识 |
| `tests/test_frontend.py`、`tests/test_admin_frontend.py` | 新增新品牌、旧品牌消失和内部标识保留契约 | 防止后续界面回退或误改兼容键 |

Notes:
- TDD：品牌契约先以 5 个预期失败点证明旧名称仍存在，分游客端和管理/API 两阶段转绿。
- Verification：`python -m pytest -q` → 306 passed；`npm test` → 39 passed；`npm run build` → 成功，222 个模块完成转换；评测集 120 题校验通过。
- 浏览器验收：游客端 5 页、管理端 5 页和 `/docs` 均显示“灵境智导”，旧品牌为 0，1280px 视口无横向溢出；未调用真实 AI、语音或路线服务。

## 12:07 比赛 Docker 镜像

Task: 制作内置当前知识库、Qdrant、景点、美食、反馈、会话和游客分析数据的 Linux/amd64 比赛镜像，并导出可离线导入的 tar。

| File | Change | Reason |
| --- | --- | --- |
| `Dockerfile` | 新增 Node 20 + Python 3.12 多阶段构建、非 root 用户、单 worker 入口和健康检查 | 生成一个启动即可演示且不携带 Node 开发运行时的比赛镜像 |
| `requirements-docker.txt` | 补齐 FastAPI、Qdrant、实时语音、LangGraph、Neo4j 和维护脚本依赖 | `pyproject.toml` 只声明了部分依赖，镜像需要完整可启动的运行集合 |
| `.dockerignore` | 排除 Git、密钥、缓存、宿主机前端产物、日志、探针和原始制作素材 | 缩小构建上下文并防止真实配置进入镜像 |
| `docs/Docker比赛镜像运行说明.md` | 增加校验、导入、密钥注入、启动、持久化和故障排查说明 | 让评委或部署人员可直接加载并运行比赛镜像 |
| `.gitignore` | 忽略导出的 `*.tar` | 保留本地比赛文件，同时避免误提交约 170 MiB 二进制归档 |
| `lingjing-ai-competition-amd64.tar.sha256` | 记录导出归档的 SHA-256 | 上传和交接后可验证文件完整性 |
| `lingjing-ai-competition-amd64.tar` | 导出 `lingjing-ai:competition` 镜像 | 提供比赛上传用的单文件离线镜像，文件本身不进入 Git |

Notes:
- Docker Hub IPv6 连接超时后，改用 AWS Public ECR 镜像的 Docker Official Node/Python 基础镜像，并以 digest 固定版本。
- Verification：`python -m compileall -q src scripts tests` → 通过；`python -m pytest -q` → 306 passed；`npm test` → 39 passed；Docker 构建阶段前端测试 39 passed、生产构建 219 modules transformed。
- 容器验证：`healthy`、重启数 0、UID 10001 非 root、`pip check` 无损坏依赖；12 个页面、Live2D、静态与媒体资源均返回 200。
- 内置数据验证：5 份资料、8 个景点、6 个美食条目、1 条反馈，游客分析接口返回 200。
- 镜像为 `linux/amd64`，大小 177,936,823 bytes；tar 为 177,964,544 bytes（169.72 MiB），SHA-256 为 `B16478C84E73C94A49059D6FC73F1C366221A6415B6C07C6B10977DCCA0B588E`，并已用 `docker load` 重新导入验证。
- `npm audit` 报告 ECharts 1 个中危运行时公告，以及仅存在于构建阶段的 Vite/esbuild 1 高危、1 中危公告；修复均要求主版本升级，本次未在比赛交付前引入未经回归的大版本变更。

## 12:47 比赛七分钟演示视频稿

Task: 编写一份可直接照读和操作的七分钟比赛演示视频稿。

| File | Change | Reason |
| --- | --- | --- |
| `docs/比赛7分钟演示视频稿.md` | 新增分秒时间轴、画面操作、完整口播、录制准备、超时删减与故障应急说明 | 帮助在七分钟内稳定展示游客服务闭环、运营管理闭环、技术架构和 Docker 交付能力 |
| `daily-modify/2026-07-20.md` | 记录本次演示稿文档变更 | 便于后续维护者追踪比赛交付材料的来源和用途 |

Notes:
- 内容已依据当前游客端五个页面、数字人交互、管理端五个页面和比赛 Docker 镜像的实际能力编写。
- Verification：人工核对各演示路径、页面名称和总时间段；本次仅新增文档，未运行代码测试。

## 13:17 游览路线回答智能组装

Task: 优化灵山胜境游玩路线问答，避免向游客输出“资料未提供完整的起点和游览顺序”等机械措辞，并直接生成可执行路线。

| File | Change | Reason |
| --- | --- | --- |
| `src/lingjing_ai/rag/answer_formatter.py` | 新增箭头路线、自然语言途经节点和方向衔接的提取与偏好匹配，并重写零散资料兜底模板 | 让经典、适老、亲子和限定时长等问题能得到路线顺序、安排理由及弹性调整建议 |
| `src/lingjing_ai/rag/generator.py` | 将游客问题传入路线组装器，扩展机械拒答识别，并强化模型侧路线规划要求 | 防止模型用检索缺口代替规划，同时保留事实约束和流式/非流式一致行为 |
| `prompt/rag_system_prompt.md` | 将“碎片化建议”升级为可执行弹性路线，明确隐藏检索过程 | 引导大模型像导游一样组装资料节点，而不是向游客解释资料是否完整 |
| `tests/test_qwen_generator.py` | 更新旧机械话术断言并新增经典路线提取回归测试 | 验证路线顺序可直接呈现，且不再出现用户指出的措辞 |
| `daily-modify/2026-07-20.md` | 记录本次路线回答优化 | 便于维护者追踪提示词、兜底逻辑和测试的联动修改 |

Notes:
- 使用真实《灵山胜境全面游览指南》验证，默认推荐可输出“入口区域 → 灵山大佛 → 九龙灌浴 → 灵山梵宫 → 五印坛城 → 祥符禅寺或其他配套景点 → 出口”。
- Verification：`python -m pytest -q` → 307 passed（20 条既有 openpyxl 弃用警告）；`git diff --check` → 通过。
