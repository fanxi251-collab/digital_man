# Daily Modify - 2026-07-18

## 12:13 Mao Pro Live2D 数字人接入

Task: 使用本地 Mao Pro Live2D 模型替换数字人模式的 SVG 形象，并增加 PCM 口型、四类语义表情、加载重试和完整本地资源服务。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/public/digital-human/live2d/` | 引入 Mao Pro 运行时、Cubism Core、样例条款、Core协议链接和来源说明 | 让模型、纹理、动作和运行库完全本地加载，并保留授权审查依据 |
| `frontend/package.json`、`frontend/package-lock.json` | 锁定 Pixi 6.5.10 与 pixi-live2d-display 0.4.0，并覆盖旧 gh-pages 传递依赖 | 提供 Cubism 4 渲染能力，同时消除本次依赖引入的 critical 安全公告 |
| `frontend/src/features/digital-human/lib/live2dLoader.js` | 按需加载本地 Core、Pixi 和 Cubism 适配器 | 常规模式不承担 Live2D 下载与初始化开销，失败后仍可重试 |
| `frontend/src/features/digital-human/lib/live2dMotion.js` | 增加音量放大、幅值限制、60/120ms平滑和动态口型参数写入 | 使用模型声明的 `ParamA` 驱动口型，避免硬编码参数和嘴型抖动 |
| `frontend/src/features/digital-human/lib/live2dExpression.js` | 增加中性、愉悦、抱歉、惊喜规则和300ms防抖 | 在不增加模型调用或协议字段时提供稳定的职业化表情 |
| `frontend/src/features/digital-human/renderers/Live2DAvatarRenderer.vue` | 新增 Pixi Live2D渲染、尺寸适配、口型钩子、表情和资源销毁 | 将具体模型渲染封装在稳定的`state/audioLevel/expression`接口后 |
| `frontend/src/features/digital-human/renderers/SvgAvatarRenderer.vue` | 删除旧SVG渲染器 | 按需求只保留 Mao Pro，不提供SVG运行时降级 |
| `frontend/src/features/digital-human/components/DigitalHumanStage.vue` | 接入Live2D、加载/错误/重试、版权标识和独立情绪文本 | 模型失败时保留字幕与语音控制，同时避免用户文本触发表情 |
| `frontend/src/components/ChatMain.vue`、`frontend/src/views/GuideView.vue` | 将助手实时字幕作为独立`emotionText`传入数字人舞台 | 表情只依据助手回答，不改变共享WebSocket与会话边界 |
| `src/lingjing_ai/api/app.py` | 将`/digital-human/`挂载到构建资源并回退public目录，保留旧Worklet地址 | 让FastAPI生产环境可返回模型、MOC3、纹理、Core和兼容资源 |
| `frontend/tests/live2d.test.js`、`tests/test_frontend.py` | 覆盖口型、表情、模块边界、本地资源和FastAPI静态路由 | 防止SVG回归、远程资源引入、参数硬编码和资源缺失 |
| `project_structure.md` | 更新数字人目录、Live2D资源和共享边界说明 | 方便后续替换模型或升级渲染器时定位职责 |

Notes:
- TDD RED：新增测试最初因缺少Live2D模块、资源、依赖和静态路由而失败。
- Full verification：`python -m pytest -q` → 250 passed，20条依赖弃用警告。
- Frontend verification：`npm --prefix frontend run test` → 19 passed。
- Production build：`npm --prefix frontend run build` → 成功，194个模块完成转换；Live2D渲染栈已拆为按需加载块。
- 浏览器验收：Mao Pro成功渲染；20次模式切换后仍只有1个Canvas；移动端无横向溢出；页面控制台无错误。
- 资源验收：Core、Pixi、Cubism、模型、MOC3、纹理、动作和表情均从`127.0.0.1:8000`加载，无第三方数字人资源请求。
- 商业或公开上线前仍需根据组织规模复核Mao样例和Cubism SDK授权条件。

## 14:14 Live2D 唇形同步时序修复

Task: 让数字人嘴型跟随浏览器实际播放队列和实时输出音量，避免服务端发送完成后语音仍播放但口型提前停止。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/src/features/digital-human/composables/usePcmAudio.js` | 增加播放活动状态、播放代次隔离、AnalyserNode时域RMS采样及统一资源清理 | 以浏览器真实播放结束作为口型结束依据，并让嘴型连续跟随扬声器输出 |
| `frontend/src/composables/useRealtimeChat.js` | 让播放生命周期控制`speaking`，完成事件不再提前待机，并保留播放期间停止操作 | 服务端发送完成早于客户端播放结束，不能作为关闭口型的依据 |
| `frontend/src/lib/realtimeProtocol.js` | 增加数字人音频状态和实时忙碌状态的纯函数 | 集中定义可测试的状态决策，降低实时会话与渲染层耦合 |
| `frontend/tests/pcm-audio.test.js` | 覆盖多分片队列、连续RMS、清理和旧播放代次回调 | 防止最后一段播放前关闭口型或旧回调干扰新回答 |
| `frontend/tests/realtime-protocol.test.js` | 覆盖服务端提前完成和播放期间停止按钮状态 | 固化传输完成不等于播放完成的行为契约 |

Notes:
- TDD RED：队列生命周期、连续分析器、服务端完成时序和播放忙碌状态测试均先因缺少对应能力失败。
- Frontend verification：`npm --prefix frontend run test` → 24 passed。
- Production build：`npm --prefix frontend run build` → 成功，197个模块完成转换。
- Full verification：`python -m pytest -q` → 272 passed，20条既有依赖弃用警告。
- 浏览器回归：实时服务在线，Mao Pro本地加载成功，控制台无新增警告或错误；未调用付费模型。

## 14:38 Mao Pro 真实口型参数修复

Task: 修复渲染器读取错误属性导致`ParamA`从未写入、数字人说话时嘴唇不动的问题。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/src/features/digital-human/lib/live2dMotion.js` | 从真实`motionManager.lipSyncIds`解析口型参数，保留旧结构回退、去重和缺失警告 | pixi-live2d-display 0.4.0不在`internalModel.lipSyncIds`保存参数，原实现始终无法写入Mao Pro的`ParamA` |
| `frontend/src/features/digital-human/renderers/Live2DAvatarRenderer.vue` | 模型加载后检查真实口型参数并对缺失情况警告一次 | 避免未来模型或适配器变更再次静默失效 |
| `frontend/src/features/digital-human/index.js` | 从数字人模块入口导出口型解析和警告能力 | 保持模块公共能力通过统一入口暴露 |
| `frontend/tests/live2d.test.js` | 使用真实渲染库对象结构，覆盖优先级、回退、去重、缺失警告和幅值限制 | 防止错误测试替身再次掩盖真实运行时结构差异 |

Notes:
- TDD RED：真实`motionManager.lipSyncIds`测试最初得到空写入；空主路径回退及缺失警告测试也先按预期失败。
- Frontend verification：`npm --prefix frontend run test` → 27 passed。
- Production build：`npm --prefix frontend run build` → 成功，197个模块完成转换。
- Full verification：`python -m pytest -q` → 272 passed，20条既有依赖弃用警告。
- 浏览器回归：Mao Pro加载成功，控制台没有口型参数警告或其他错误；未调用付费模型。

## 21:16 三角色 Live2D 数字人并入

Task: 将单一Mao Pro扩展为Mao Pro、Chitose和Haruto三个本地角色，并按角色绑定受控音色和轻量表达风格。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/public/digital-human/live2d/chitose/`、`frontend/public/digital-human/live2d/haruto/` | 引入两个模型的运行时文件、来源和授权说明，并为Haruto补充LipSync/EyeBlink组 | 只发布浏览器渲染所需资源，同时让Haruto的标准嘴型和眨眼参数可被SDK识别 |
| `frontend/src/features/digital-human/lib/live2dCharacters.js` | 新增三角色注册表、语义表情映射、布局参数和localStorage白名单 | 让角色配置集中管理，并阻止无效本地偏好进入渲染与协议 |
| `DigitalHumanStage.vue`、`Live2DAvatarRenderer.vue` | 增加角色按钮、动态模型加载、版权文字和旧实例完整销毁 | 切换时同一时刻只保留一个Canvas、Ticker和模型实例 |
| `useRealtimeChat.js`、`ChatMain.vue`、`GuideView.vue` | 同步角色选择、保存浏览器偏好，并在服务端确认前锁定数字人提交入口 | 防止新问题在音色/角色尚未绑定时发往上游，同时保留共享历史 |
| `src/lingjing_ai/realtime/avatar_profiles.py` | 建立Mao、Chitose、Haruto服务端白名单及音色/表达映射 | 客户端只能传角色ID，不能注入任意voice或提示词 |
| `session.py`、`qwen_audio.py`、`conversation.py` | 增加`avatar.set/avatar.changed`、轮次角色快照、逐轮voice和临时角色表达 | 切换不调用模型；生成中的轮次不会中途换音色，角色提示也不进入持久化历史 |
| `settings.py` | 增加三角色音色默认值和可选环境变量覆盖 | 统一维护Mao `longanqian`、Chitose `longanlufeng`、Haruto `longanxiaoxin`映射 |
| 前后端测试 | 覆盖资源完整性、表情映射、偏好恢复、非法角色、切换取消、逐轮音色和静态资源访问 | 固化三角色边界，并确保常规模式不携带voice或请求音频 |
| `project_structure.md`、`docs/qwen_audio_realtime.md` | 更新数字人资源、角色注册、音色配置和后端白名单职责 | 便于后续替换模型或增加角色时沿用低耦合边界 |

Notes:
- TDD RED：后端角色模块、三角色静态资源访问和角色同步提交锁测试均先按预期失败，再实现最小能力。
- Frontend verification：`npm --prefix frontend run test` → 31 passed。
- Production build：`npm --prefix frontend run build` → 成功，195个模块完成转换。
- Full verification：`python -m pytest -q` → 283 passed，20条既有依赖弃用警告。
- 原始`chitose_ja/`和`koharu_haruto_ja/`下载目录保持不变，Koharu没有并入生产资源。
- 正式商用前仍需重新核对Live2D样例数据使用条款、组织规模限制和版权标识要求。
