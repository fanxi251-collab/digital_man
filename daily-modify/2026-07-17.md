# Daily Modify - 2026-07-17

## 11:17 数字人语音识别增强

Task: 增加浏览器录音质量控制、景区领域词典、转写纠错和低置信度确认流程。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/public/pcm-capture-worklet.js` | 为每个100ms PCM分片计算RMS、峰值和削波比例 | 在发送音频的同时获得输入质量指标，避免复制或保存录音 |
| `frontend/src/lib/audioCaptureQuality.js` | 新增音量阈值、质量统计和300ms尾部计时器 | 统一判断静音、低音量、严重削波和句尾保护 |
| `frontend/src/lib/pcmAudio.js` | 增加Float32输入指标计算 | 为工作线程算法提供可测试的等价实现 |
| `frontend/src/lib/realtimeProtocol.js` | 增加转写确认事件构造器 | 保证候选选择和人工编辑使用统一协议 |
| `frontend/src/composables/usePcmAudio.js` | 请求自动增益并暴露输入音量、质量和浏览器支持状态 | 改善麦克风输入并将采集音量与数字人口型音量分离 |
| `frontend/src/composables/useRealtimeChat.js` | 接入尾部保护、提交拦截、纠错标记和确认事件 | 防止截断句尾，并在低置信度时暂停回答等待用户确认 |
| `frontend/src/components/ChatMain.vue` | 增加音量提示、尾部状态和可编辑候选确认界面 | 让游客能理解录音质量并处理歧义转写 |
| `frontend/src/views/GuideView.vue`、`frontend/src/styles.css` | 传递新增状态并补充确认界面样式 | 完成数字人页面集成 |
| `config/asr_glossary.yml` | 增加景区标准词、别名、拼音和权重示例 | 提供可人工维护且优先级最高的领域词典 |
| `src/lingjing_ai/realtime/glossary.py` | 合并已发布景区、标签、文档标题和YAML词条 | 自动形成内存词典并支持60秒缓存和YAML热重载 |
| `src/lingjing_ai/realtime/transcript.py` | 增加别名纠正、拼音窗口匹配、保护词和置信度分级 | 只修改词典支持的片段，降低误纠风险 |
| `src/lingjing_ai/services/question_expansion.py` | 扩展语音理解结构化输出 | 复用一次qwen3.7-plus调用完成上下文消歧和问题扩写 |
| `src/lingjing_ai/realtime/conversation.py` | 增加语音预处理并允许复用扩写结果 | 确认前不创建会话，正常语音轮次不增加模型调用 |
| `src/lingjing_ai/realtime/session.py` | 接入纠错、上游消息替换、候选确认和脱敏日志 | 只把确认文本交给 Agent、Qwen 上下文和持久化历史 |
| `src/lingjing_ai/config/settings.py`、`src/lingjing_ai/api/app.py` | 增加纠错配置并装配词典服务 | 让实时会话默认启用新能力且支持配置覆盖 |
| `pyproject.toml` | 声明`pypinyin`和`PyYAML`依赖 | 支持真实拼音转换和YAML词典解析 |
| `tests/test_asr_glossary.py`、`tests/test_transcript_normalizer.py` | 覆盖词典合并、人工覆盖、拼音纠错和置信度 | 防止领域词典和确定性纠错回归 |
| `tests/test_realtime_session.py`、`tests/test_realtime_conversation.py`、`tests/test_question_expansion.py` | 覆盖纠正后持久化、低置信度确认和单次扩写 | 验证实时协议和历史一致性 |
| `frontend/tests/*.test.js`、`tests/test_frontend.py`、`tests/test_settings.py` | 覆盖采集质量、尾部计时、确认协议、页面装配和配置 | 验证前端与配置行为 |
| `docs/qwen_audio_realtime.md` | 记录新配置、音频质量处理和纠错协议 | 方便部署与后续维护 |

Notes:
- Installed `pypinyin 0.55.0` into the active Python/Conda environment.
- Verification: `python -m pytest -q` → 218 passed, 20 dependency deprecation warnings.
- Verification: `npm --prefix frontend run test` → 12 passed.
- Verification: `npm --prefix frontend run build` → successful production build.
- The planned 36-item real-audio acceptance corpus still requires consented Mandarin, Wuxi-accented Mandarin, and noisy recordings supplied or recorded by the project team; no visitor audio was fabricated or persisted in this task.

## 12:23 智能导游回答丰富度与完整路线优化

Task: 为常规/数字人模式增加独立回答契约，并把高德路线压缩为可校验、可绘制、可持久化的 V2 摘要。

| File | Change | Reason |
| --- | --- | --- |
| `src/lingjing_ai/tools/route_summary.py` | 新增关键步骤选择、RDP轨迹简化、500点上限和V2摘要构建 | 保留完整地图形状与8—12条关键步骤，同时控制接口和历史体积 |
| `src/lingjing_ai/tools/amap_tools.py` | 使用第一推荐路线生成V2摘要、紧凑兼容字段和`amap:route:v2:*`缓存 | 避免重复保存原始108步和12052点路线对象，也避免复用旧缓存 |
| `src/lingjing_ai/realtime/answer_contract.py` | 新增四类回答契约、短回答资料补充、路线完整性校验和失败降级 | 提高常规回答丰富度，并在不增加模型调用时保证路线字段完整 |
| `src/lingjing_ai/realtime/conversation.py`、`src/lingjing_ai/realtime/session.py` | 按模式准备契约，并把确定性补充作为同一文本流后缀发送和持久化 | 保证界面最终文本、持久化历史与结构化路线一致 |
| `src/lingjing_ai/config/settings.py` | 移除全局2—4句限制，改为遵守逐轮回答契约 | 防止数字人口播限制继续压缩常规模式回答 |
| `src/lingjing_ai/services/tool_intent.py`、`src/lingjing_ai/agent/executor.py` | 缺少明确起点或终点时先澄清且不调用高德 | 防止不完整地点进入地图工具或被RAG资料冒充实时路线 |
| `frontend/src/lib/routeSummary.js` | 统一解析V2与旧版路线来源并识别成功路线 | 让导游页和历史来源保持向后兼容 |
| `frontend/src/composables/useRealtimeChat.js`、`frontend/src/composables/useChat.js` | 仅把具有可绘制数据的来源视为成功路线 | 路线失败时不自动打开空地图 |
| `frontend/src/views/GuideView.vue`、`frontend/src/components/RoutePanel.vue` | 成功后自动打开路线面板，展示总览、完整关键步骤和原始步骤数量 | 让导游路线体验与互动地图保持一致 |
| `frontend/src/views/MapView.vue`、`frontend/src/styles.css` | 取消前8步截断并补充路线摘要样式 | 完整展示后端筛选出的8—12条关键步骤 |
| `tests/test_route_summary.py`、`tests/test_amap_tools.py` | 覆盖108步提炼、12052点压缩、100KB限制和V2缓存 | 防止路线体积和关键转向选择回归 |
| `tests/test_answer_contract.py`、`tests/test_realtime_conversation.py`、`tests/test_realtime_session.py` | 覆盖回答档位、短答补充、路线字段补齐、流式发送与持久化 | 验证全流程仍只使用一次最终回答调用 |
| `frontend/tests/route-summary.test.js`、`tests/test_frontend.py`、`tests/test_agent_evidence.py` | 覆盖新旧来源、自动面板、失败不切换和缺地点澄清 | 固化前后端路线体验边界 |
| `docs/qwen_audio_realtime.md`、`project_structure.md` | 记录回答契约、路线V2和新增模块 | 方便后续维护与部署排查 |

Notes:
- Verification: `python -m pytest -q` → 236 passed, 20 dependency deprecation warnings.
- Verification: `npm --prefix frontend run test` → 14 passed.
- Verification: `npm --prefix frontend run build` → successful production build (47 modules transformed).
- No existing route history was migrated or deleted; old metadata remains readable through the frontend compatibility resolver.

## 12:52 景区内部路线历史覆盖修复

Task: 修复完整“从A到B”路线被历史目标覆盖的问题，并让景区内部路线使用已发布景点坐标和步行默认方式。

| File | Change | Reason |
| --- | --- | --- |
| `src/lingjing_ai/services/conversation.py` | 完整路线问题优先保留原文 | 防止历史中的“灵山大佛”等目标覆盖“五明桥到五智门” |
| `src/lingjing_ai/services/attraction_store.py` | 增加已发布景点名称精确查询 | 草稿、归档和模糊名称不能进入游客路线坐标解析 |
| `src/lingjing_ai/tools/amap_tools.py` | 接入可选地点解析器、内部默认步行并只地理编码缺失端点 | 避免高德命中景区外同名地点，同时保留外部路线驾车默认值 |
| `src/lingjing_ai/api/app.py` | 将同一景点坐标解析器注入实时Agent和地图HTTP接口 | 保证对话路线与互动地图使用相同的本地坐标事实源 |
| `tests/test_conversation_context.py`、`tests/test_realtime_conversation.py` | 覆盖带历史目标的完整内部路线 | 固化原历史会话中的实际回归场景 |
| `tests/test_attraction_store.py`、`tests/test_amap_tools.py` | 覆盖发布状态、内部步行、显式驾车和混合端点 | 验证地点解析与方式优先级 |
| `tests/test_amap_api.py`、`tests/test_agent_evidence.py` | 覆盖应用装配、HTTP接口和单次Agent路线证据 | 防止只有工具单测通过但真实链路未注入解析器 |

Notes:
- Verification: `python -m pytest -q` → 246 passed, 20 dependency deprecation warnings.
- Verification: `npm --prefix frontend run test` → 14 passed.
- Verification: `npm --prefix frontend run build` → successful production build (47 modules transformed).
- Existing conversation history was preserved without migration or deletion.
- Restarted Uvicorn from the current `LJ` Conda environment at 12:56:03; `/visitor/guide` returned HTTP 200.
- Replayed the original historical session: the unqualified internal route used published coordinates, planned one walking route (about 212 m / 3 min), and persisted the complete answer; explicit driving overrode the default (about 213 m / 2 min).

## 14:39 数字人前端独立模块重构

Task: 将数字人专属渲染、语音控件、PCM音频和采集质量逻辑集中到独立前端功能目录，同时保持双模式共享实时会话边界。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/src/features/digital-human/` | 新增公共入口、舞台、SVG渲染器、语音控件、转写确认、PCM与质量模块 | 集中数字人专属职责，并以稳定的`state`/`audioLevel`契约支持未来替换Live2D或3D |
| `frontend/src/components/ChatMain.vue`、`frontend/src/composables/useRealtimeChat.js` | 改为只通过数字人模块入口使用专属组件和音频能力 | 防止共享聊天层依赖数字人内部路径，同时保留常规与数字人共用会话 |
| `frontend/public/digital-human/pcm-capture-worklet.js`、`src/lingjing_ai/api/app.py` | 使用功能域资源路径并兼容旧Worklet URL | 让Vite开发和FastAPI生产使用同一规范路径，避免缓存中的旧页面失效 |
| `frontend/src/styles.css` | 移除已迁入组件的数字人舞台与转写确认全局样式 | 降低全局CSS与数字人渲染实现之间的耦合 |
| `frontend/tests/*.test.js`、`tests/test_frontend.py` | 更新模块路径并覆盖公共入口、渲染器契约和新旧Worklet地址 | 固化重构边界并防止录音、口型和资源兼容行为回归 |
| `project_structure.md`、`docs/qwen_audio_realtime.md` | 记录数字人目录职责与共享层边界 | 方便后续增加Live2D或3D渲染器时定位扩展点 |

Notes:
- TDD red phase: the focused Python suite failed on the missing feature directory, missing renderer, and 404 canonical Worklet route.
- Focused verification: `python -m pytest tests/test_frontend.py -q` → 13 passed.
- Focused verification: `npm --prefix frontend run test` → 14 passed.
- Full verification: `python -m pytest -q` → 248 passed, 20 dependency deprecation warnings.
- Production build: `npm --prefix frontend run build` → successful, 56 modules transformed.
- Browser regression: both modes rendered and switched correctly, the SVG stage and hold-to-talk control were present, and no console errors were recorded.
- Worklet regression: canonical and compatibility URLs both returned HTTP 200 with identical processor content.
- Restarted the local Uvicorn service from the `LJ` Conda environment at 14:43:11 (PID 50080) for manual verification.
- Existing uncommitted work was preserved; no mixed Git commit was created during this refactor.
