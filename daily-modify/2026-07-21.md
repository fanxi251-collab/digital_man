# Daily Modify - 2026-07-21

## 17:20 业务库迁到 PostgreSQL AgentDB

Task: 将会话、景点、美食、反馈四组遗留关系数据迁入本机 PostgreSQL 库 `AgentDB`，按 schema 隔离。

| File | Change | Reason |
| --- | --- | --- |
| `src/lingjing_ai/storage/postgres.py` | 新增连接、schema 初始化与查询辅助 | 统一 PostgreSQL 访问，避免四套重复连接逻辑 |
| `src/lingjing_ai/services/conversation_store.py` | 改用 psycopg，schema=`conversations` | 会话历史改为 AgentDB |
| `src/lingjing_ai/services/attraction_store.py` | 改用 psycopg，schema=`attractions` | 景点与图集元数据改为 AgentDB |
| `src/lingjing_ai/services/food_store.py` | 改用 psycopg，schema=`foods` | 美食推荐改为 AgentDB |
| `src/lingjing_ai/services/feedback_store.py` | 改用 psycopg，schema=`feedback` | 游客反馈改为 AgentDB |
| `src/lingjing_ai/config/settings.py`、`config.yml` | 增加 `DATABASE_URL` / `DATABASE_SCHEMA_PREFIX` | 运行时可配置连接串；测试可用前缀隔离 |
| `src/lingjing_ai/api/app.py` | Store 改为按 settings 连接 PostgreSQL | 应用启动统一写入 AgentDB |
| `pyproject.toml`、`requirements-docker.txt` | 增加 `psycopg[binary]` | 安装 PostgreSQL 驱动 |
| `tests/conftest.py` 及相关 store/API 测试 | 每测独立 schema 前缀；构造函数改传 DSN | 避免污染生产 schema，并覆盖迁移后行为 |
| `scripts/refresh_attraction_seed_covers.py` | 改为从 settings 读取 AgentDB | 与新存储路径一致 |

Notes:
- 连接默认：`postgresql://postgres:523@localhost:5432/AgentDB`
- Schema：`conversations` / `attractions` / `foods` / `feedback`；空库仍会自动 seed 景点 8 条、美食 6 条
- Verification：相关 pytest 32 passed；冒烟初始化后 AgentDB 中 attractions=8、foods=6
- 遗留关系数据文件不再被应用写入，备份已移出工作区保存
