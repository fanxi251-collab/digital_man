# Daily Modify - 2026-07-30

## 09:09 Arcane 3D 女导游接入

Task: 将默认女导游从 Mao Pro Live2D 替换为本地 Arcane 3D，同时保留 Chitose 与 Haruto Live2D。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/package.json` | 固定新增 `three@0.180.0` | 为 Arcane 提供本地 Three.js 与 GLTFLoader 渲染能力。 |
| `frontend/package-lock.json` | 锁定 Three.js 依赖 | 保证开发与比赛展示环境使用一致版本。 |
| `frontend/src/features/digital-human/lib/avatarProfiles.js` | 新增通用 3D/Live2D 角色注册表 | 让同一舞台按角色选择渲染器，同时保留 `mao_pro` 兼容 ID。 |
| `frontend/src/features/digital-human/lib/live2dCharacters.js` | 改为兼容性转发入口 | 避免旧导入路径在通用角色注册表重构后失效。 |
| `frontend/src/features/digital-human/lib/audioDrivenMouth.js` | 抽取共享音量口型平滑 | 让 3D 与 Live2D 共用 60ms 张嘴、120ms 闭嘴规则。 |
| `frontend/src/features/digital-human/lib/threeMotion.js` | 新增 3D 状态、表情与 Morph Target 控制 | 将实时音量和语义状态映射到 Arcane 动作及面部目标。 |
| `frontend/src/features/digital-human/renderers/ThreeDAvatarRenderer.vue` | 新增按需 Three.js 渲染器 | 加载 GLB、播放五种动作、驱动口型/表情并完整释放 WebGL 资源。 |
| `frontend/src/features/digital-human/renderers/Live2DAvatarRenderer.vue` | 改用通用角色注册表 | 保持男导游与儿童导游 Live2D 行为不变。 |
| `frontend/src/features/digital-human/components/DigitalHumanStage.vue` | 按 `rendererType` 切换 3D 或 Live2D | 在不修改实时会话接口的前提下支持混合渲染。 |
| `frontend/src/features/digital-human/components/DigitalHumanAvatarSelector.vue` | 使用通用角色资料 | 正确显示 Arcane、Chitose 与 Haruto。 |
| `frontend/src/features/digital-human/index.js` | 导出通用角色注册能力 | 保持数字人模块单一公共入口。 |
| `frontend/src/lib/realtimeProtocol.js` | 读取通用角色白名单 | 保持既有角色协议与 `mao_pro` ID 兼容。 |
| `frontend/src/lib/realtimeSessionEvents.js` | 读取通用角色白名单 | 保持会话确认和角色同步行为不变。 |
| `tools/digital-human/arcane/arcane-rig-map.json` | 记录源模型校验值和关键骨骼索引 | 防止源模型变化后错误套用离线加工规则。 |
| `tools/digital-human/arcane/refine_arcane_glb.mjs` | 新增可重复的离线 GLB 加工流程 | 生成五种状态动作和六个面部目标，不修改用户原始模型。 |
| `frontend/public/digital-human/3d/arcane/arcane-guide.glb` | 生成网页运行版 Arcane 模型 | 提供内嵌纹理、蒙皮、状态动作、眨眼、表情和口型目标。 |
| `frontend/public/digital-human/3d/arcane/model-manifest.json` | 新增运行资产清单 | 记录动作、Morph Target、模型规模、作者与许可。 |
| `frontend/public/digital-human/3d/arcane/ATTRIBUTION.md` | 新增模型署名与 IP 风险说明 | 满足 CC BY 4.0 署名并提示游戏衍生资源授权边界。 |
| `src/lingjing_ai/realtime/avatar_profiles.py` | 将 `mao_pro` 显示身份改为 Arcane 女导游 | 保持原角色 ID、女声音色与表达风格不变。 |
| `src/lingjing_ai/api/app.py` | 增加混合数字人静态资源完整性检查 | 避免旧构建缺少 Arcane 时产生部分资源 404。 |
| `frontend/tests/three-avatar.test.js` | 新增 3D 口型、动作和表情测试 | 防止状态映射及音量同步回归。 |
| `frontend/tests/live2d-characters.test.js` | 更新三角色注册断言 | 验证 Arcane 为默认 3D，另外两角色仍为 Live2D。 |
| `tests/test_arcane_assets.py` | 新增真实 GLB、授权和静态资源测试 | 验证运行模型大小、动作、Morph Target 和本地资源契约。 |
| `tests/test_frontend.py` | 增加混合渲染与 Arcane 静态资源断言 | 覆盖前后端集成边界。 |
| `tests/test_realtime_avatar_profiles.py` | 更新女导游身份断言 | 验证后端仍用兼容角色 ID 和既有音色。 |
| `project_structure.md` | 记录 3D/Live2D 混合模块结构 | 便于后续维护、回滚和替换原创模型。 |

Notes:
- 原始 `arcane-arknights-endfield/` 与 `arcane__arknights_endfield.glb` 保持不动。
- 运行版 GLB 为 15,829,072 字节，生产构建与 public 资源 SHA-256 一致。
- 验证通过：`npm --prefix frontend run test`（60 passed）、`npm --prefix frontend run build`、`python -m pytest -q`（205 passed, 123 skipped）。
- 浏览器验收确认 Arcane 正常显示、仅存在一个 Three.js Canvas、无 3D 加载错误；临时 Vite 服务验收后已停止。
- Blender 便携包下载未完成校验，本次可运行资产由仓库内离线 glTF 加工脚本生成。

## 09:41 移除 Arcane 3D 并恢复 Mao Pro

Task: 根据实际演示效果移除项目生成的 Arcane 3D 模块，将默认女导游恢复为 Mao Pro Live2D。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/package.json`、`frontend/package-lock.json` | 移除 Three.js 依赖 | 纯 Live2D 运行时不再需要3D渲染库。 |
| `frontend/src/features/digital-human/lib/live2dCharacters.js` | 恢复三角色 Live2D 注册表 | 重新启用 Mao Pro，并保持 Chitose、Haruto 不变。 |
| `frontend/src/features/digital-human/components/DigitalHumanStage.vue` | 恢复单 Live2D 渲染器舞台 | 删除3D分流，避免加载低质量3D模型。 |
| `frontend/src/features/digital-human/`、`frontend/public/digital-human/3d/` | 删除生成的3D渲染、动作、口型和运行资源 | 完整移除 Arcane 3D 运行路径。 |
| `src/lingjing_ai/realtime/avatar_profiles.py` | 恢复 Mao 女导游身份 | 继续使用原女性音色和表达规则。 |
| `src/lingjing_ai/api/app.py` | 恢复三角色 Live2D 静态资源检查 | 防止旧构建或不完整角色资源被加载。 |
| `frontend/tests/`、`tests/` | 删除3D专项测试并恢复 Mao 回归断言 | 让测试覆盖当前实际使用的纯 Live2D 架构。 |
| `project_structure.md` | 恢复 Live2D-only 项目说明 | 使文档与当前运行结构一致。 |

Notes:
- 根目录原始 Arcane 下载文件未被修改或删除，由用户自行处理。
- `ExploreView.vue`、`.codex/`、`.cursor/`及其他无关改动均未触碰。

## 10:13 完整移除 Haruto 儿童导游

Task: 将数字人角色缩减为 Mao Pro 女导游和 Chitose 男导游，完整移除 Haruto。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/src/features/digital-human/lib/live2dCharacters.js` | 删除 Haruto 角色资料 | 前端选择器和本地偏好仅允许女、男两名导游。 |
| `src/lingjing_ai/realtime/avatar_profiles.py` | 删除 Haruto 白名单、音色和表达规则 | 服务端拒绝旧 Haruto 角色请求，避免继续建立儿童音色连接。 |
| `src/lingjing_ai/config/settings.py` | 删除 `realtime_voice_haruto` 配置 | 不再读取或维护已移除角色的环境变量。 |
| `src/lingjing_ai/api/app.py` | 静态资源完整性检查缩减为两角色 | 生产构建不再要求 Haruto 目录。 |
| `frontend/public/digital-human/live2d/haruto/` | 逐项删除模型、纹理、动作和说明文件 | 完整移除儿童导游运行资源。 |
| `frontend/tests/`、`tests/` | 更新为双角色断言并增加旧 Haruto 请求拒绝测试 | 防止角色重新出现或旧偏好继续生效。 |
| `docs/qwen_audio_realtime.md`、`docs/项目部署运行配置说明书.md`、`project_structure.md` | 删除 Haruto 音色和角色说明 | 使部署配置和项目文档与实际角色集合一致。 |

Notes:
- 旧浏览器若保存了 `haruto`，前端会自动回退到默认 Mao Pro。

## 11:05 Haru Greeter替换默认女导游

Task: 保留兼容角色ID、女声音色和Mao Pro回滚资源，将默认女导游的可见Live2D形象替换为Haru Greeter。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/public/digital-human/live2d/haru_greeter/` | 新增34个Haru原始运行文件和项目README | 将用户提供的Haru Greeter全部本地部署，并记录来源与许可。 |
| `frontend/src/features/digital-human/lib/live2dCharacters.js` | 将兼容ID `mao_pro`指向Haru模型，配置空表情映射和待机动作 | 不迁移协议与浏览器偏好，同时适配Haru没有表情文件、动作组名称为空的真实资源结构。 |
| `frontend/src/features/digital-human/lib/live2dMotion.js` | 新增按角色启动待机动作的安全函数 | 让Haru在禁用自动交互时仍播放官方待机动作，且可选动作失败不会阻断语音和口型。 |
| `frontend/src/features/digital-human/renderers/Live2DAvatarRenderer.vue` | 模型加载完成后启动角色配置的待机动作 | 使Haru上线后不是静止形象，并保持Chitose原行为。 |
| `src/lingjing_ai/realtime/avatar_profiles.py` | 将女导游显示身份更新为Haru | 保持`mao_pro`白名单ID与`longanqian`音色不变，只同步角色身份。 |
| `src/lingjing_ai/api/app.py` | 静态资源完整性检查同时要求Haru、Mao和Chitose | 防止生产构建遗漏当前女导游或Mao回滚资源。 |
| `frontend/tests/live2d-characters.test.js`、`frontend/tests/live2d.test.js` | 增加Haru注册、无表情降级和待机动作测试 | 防止未来重新指向Mao或因动作组为空而不播放待机。 |
| `tests/test_frontend.py`、`tests/test_realtime_avatar_profiles.py` | 增加Haru资源、静态访问、口型声明、动作数量和后端身份断言 | 保证本地运行资源完整且女声音色兼容。 |
| `frontend/public/digital-human/live2d/NOTICE.md`、`docs/qwen_audio_realtime.md`、`project_structure.md` | 更新Haru、Mao回滚和双角色说明 | 使运行架构、授权信息和连接级音色文档与代码一致。 |
| `docs/superpowers/plans/2026-07-30-replace-mao-with-haru-greeter.md` | 保存测试驱动实施计划 | 记录兼容替换边界和验证步骤，便于审查与回滚。 |

Notes:
- 原始目录`haru_greeter_ja/`未修改，Mao Pro运行资源未删除。
- RED阶段前端3项与后端1项按预期失败；最小实现后前端58项全部通过，后端角色测试通过。
- 完整验证：`npm --prefix frontend run test`为58 passed；`npm --prefix frontend run build`成功构建228个模块；`python -m pytest -q`为202 passed、123 skipped。
- 构建产物包含Haru 35个、Mao 22个、Chitose 18个文件；Haru模型无远程URL、无缺失引用。内置浏览器因隔离网络无法访问本机5176端口，视觉截图留待浏览器人工刷新验收。

## 11:38 修复Haru首次加载手臂虚影与默认首帧跳变

Task: 修复首次进入数字人模式时，Haru两套手臂短暂重叠，以及MOC默认姿态先于待机动作显示的问题。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/public/digital-human/live2d/haru_greeter/haru_greeter_t05.pose3.json` | 将左右手臂Pose默认顺序从B优先改为A优先 | 与待机动作第0秒的A手臂可见状态保持一致，避免0.5秒Pose淡入同时绘制两套手臂。 |
| `frontend/public/digital-human/live2d/haru_greeter/haru_greeter_t05.model3.json` | 将Haru待机动作淡入时间显式设为0.01秒 | 渲染库默认500ms动作淡入会把MOC初始交叉手臂与待机下垂手臂混合，形成视频中的双手臂虚影。 |
| `frontend/public/digital-human/live2d/haru_greeter/README.md` | 记录项目副本的Pose与待机淡入兼容修正 | 说明部署资源相对原始Haru包的有意差异，便于授权审查和回滚。 |
| `frontend/tests/live2d-assets.test.js` | 新增Pose首帧一致性与待机短淡入测试 | 防止资源替换后再次出现默认姿态不一致或互斥手臂交叉淡入。 |
| `frontend/src/features/digital-human/lib/live2dMotion.js` | 新增首帧显示门控及“Pose重置后再启动待机动作”的统一初始化函数 | Pixi异步加载动作前会渲染MOC默认姿态；动作若先于Pose初始化还会捕获错误手臂作为淡入起点。 |
| `frontend/src/features/digital-human/renderers/Live2DAvatarRenderer.vue` | Haru加载时先重置Pose、再启动待机动作，最后开放首帧显示 | 从初始化顺序上消除交叉手臂起点，并保证角色切换和卸载不会让旧Canvas延迟显现。 |
| `frontend/tests/live2d.test.js` | 增加首帧门控、取消时序及Pose早于待机动作的调用顺序测试 | 防止渲染器再次捕获MOC默认手臂或在动作生效前暴露Canvas。 |
| `frontend/dist/` | 重新生成包含Pose与首帧门控修复的生产资源 | FastAPI优先提供dist，必须同步构建才能让当前页面实际使用修复。 |

Notes:
- 第一轮RED测试准确报告`pose starts Part01ArmRB001, but idle starts Part01ArmRA001`；第二轮RED测试确认缺少稳定首帧门控。
- 用户录屏逐帧确认剩余虚影来自待机动作默认500ms淡入；生产页面0ms、80ms、200ms、450ms截图复验均只显示一套手臂。
- 最终采用Pose预初始化根因修复后，前端63项测试全部通过；生产构建成功；后端回归为202 passed、123 skipped。
- 8000端口确认加载新入口`index-CICB-Zsp.js`；首次可见后的连续10帧均只有一套手臂，浏览器控制台无警告或错误。
- 当前8000端口返回的Pose为`RA→RB / LA→LB`，HTTP状态为200。
- 根目录`haru_greeter_ja/`未修改。
- 服务端收到 `avatar.set: haruto` 时返回 `INVALID_AVATAR`。

## 16:04 Haru女导游语义动作与互斥调度

Task: 根据游客原话、助手回答、路线证据和实时状态自动触发Haru动作，采用“具体语义优先、同义动作轮换”，并保证同一时间只有一个身体动作。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/src/features/digital-human/lib/live2dSemanticMotion.js` | 新增语义动作解析、300ms流式防抖和同义动作轮换 | 让游客夸奖、抱怨、赞同、惊讶等语义稳定映射到一个最具体动作，不增加模型调用。 |
| `frontend/src/features/digital-human/lib/live2dCharacters.js` | 为Haru配置M00及用户注释的动作索引、时长和M22/M23变体 | 集中声明可用动作，并确保M03、M06、M11、M14、M15、M24不会触发。 |
| `frontend/src/features/digital-human/lib/live2dMotion.js` | 新增FORCE优先级单动作调度器、播放代次和一次性动作回退 | 原子替换旧动作，防止循环动作、旧定时器和新状态同时控制模型。 |
| `frontend/src/lib/realtimeTurnHelpers.js`、`frontend/src/composables/useRealtimeChat.js` | 暴露最近一条有效游客消息 | 动作选择需要同时理解游客语气与助手回答，避免只根据助手字幕误判。 |
| `frontend/src/views/GuideView.vue`、`frontend/src/components/ChatMain.vue`、`frontend/src/features/digital-human/components/DigitalHumanStage.vue` | 传递游客文本、路线状态并解析语义动作 | 保持共享实时会话不变，只在数字人舞台增加低耦合动作上下文。 |
| `frontend/src/features/digital-human/renderers/Live2DAvatarRenderer.vue` | 初始化Haru欢迎动作并接入互斥动作调度器 | 保留Pose首帧修复和口型写入顺序，同时让状态动作能够安全抢占。 |
| `frontend/tests/live2d-semantic-motion.test.js`、`frontend/tests/live2d.test.js`、`frontend/tests/live2d-characters.test.js`、`frontend/tests/realtime-session-events.test.js` | 增加语义优先级、轮换、排除动作、互斥、旧回调隔离和游客文本测试 | 防止随机误触发、动作重叠或未来重新启用排除动作。 |
| `docs/superpowers/plans/2026-07-30-haru-semantic-motions.md`、`project_structure.md` | 保存实施计划并更新模块边界说明 | 便于后续替换关键词、动作或Live2D模型时保持单一职责。 |

Notes:
- Chitose男导游没有接入Haru语义动作配置，现有行为保持不变。
- Qwen、WebSocket协议、后端、数据库、音频与口型参数均未修改。
- RED阶段按预期因缺少语义模块、动作配置和调度器失败；边界测试另行确认服务故障优先M16，普通天气建议不会误触发M20。
- 最终验证：前端75项测试通过；Vite构建226个模块成功；Python为202 passed、123 skipped；FastAPI实际提供入口`index-DytHBXo7.js`，Haru模型资源返回200。

## 16:47 移除五个不合适的Haru自动动作

Task: 根据动作确认结果，停止自动触发M04、M05、M12、M16和M25，同时保留原始模型动作文件用于复核。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/src/features/digital-human/lib/live2dCharacters.js` | 从自动动作配置中移除五个编号，并将相关语义映射到M13、M26、M19和M09 | 避免继续播放用户确认不合适的动作，同时防止命中语义后残留上一个身体动作。 |
| `frontend/tests/live2d-characters.test.js` | 增加五个编号永不进入自动配置的断言，并验证替代动作 | 防止后续调整无意恢复已排除动作。 |
| `test-artifacts/haru-motion-annotator.html` | 将五个动作标记为“已排除·可复核” | 让临时动作确认器与正式自动动作集合保持一致，但仍允许人工预览。 |
| `frontend/dist/` | 重新生成生产前端 | FastAPI优先提供构建产物，需要同步发布新的动作注册表。 |

Notes:
- 动作资源文件未删除；本次只改变自动选择行为。
- RED阶段确认旧配置仍将赞同映射到M04；修改后定向测试10项通过。
- 完整前端测试75项通过，Vite生产构建成功并生成入口`index-a9woYdh8.js`。

## 17:02 普通讲解动作确定性轮换

Task: 扩充普通讲解的动作表现，并保证相同问题的多轮回答能够稳定轮换动作。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/src/features/digital-human/lib/live2dCharacters.js` | 将普通讲解配置为M07、M22、M07、M23、M21的循环变体，路线继续固定M07 | 普通回答不再总是使用M07，同时让讲解动作占40%，保持导游表达的专业感。 |
| `frontend/tests/live2d-characters.test.js` | 验证讲解轮换顺序、持续到本轮结束和路线固定动作 | 防止动作池比例或路线专业动作被无意改变。 |
| `frontend/tests/live2d.test.js` | 增加连续五轮讲解和下一轮路线的集成级调度测试 | 证明每轮只选择一个动作、跨轮按序变化且路线不参与普通轮换。 |
| `frontend/dist/` | 重新生成生产前端 | 让FastAPI静态页面加载新的轮换配置。 |

Notes:
- 特定害羞、惊讶、道歉、纠结和欢迎语义仍优先于普通讲解，并通过现有FORCE优先级互斥替换。
- RED阶段确认旧配置连续五轮均为M07；修改后定向测试20项通过。
- 完整前端测试76项通过，Vite生产构建成功并生成入口`index-Bz8TiR0m.js`。
