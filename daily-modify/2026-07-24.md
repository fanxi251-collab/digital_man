# Daily Modify - 2026-07-24

## 09:27 游客端代码结构优化

Task: 拆分大 composable、去重高德加载与游客目录请求、清理 Vue SPA 死代码，覆盖五个游客页。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/src/lib/amapLoader.js` | 新增共用 `fetchMapConfig` / `loadAmapScript` / `parseLngLat` | 消除互动地图与路线卡片重复加载逻辑 |
| `frontend/src/composables/useInteractiveMap.js` | 改为引用 `amapLoader` | 地图页只保留实例与标记职责 |
| `frontend/src/composables/useRouteMap.js` | 改为引用 `amapLoader` | 与互动地图共享同一脚本加载单例 |
| `frontend/tests/amap-loader.test.js` | 新增坐标解析与并发加载单测 | 回归共用加载器行为 |
| `frontend/src/lib/realtimeTurnHelpers.js` | 抽出消息态纯函数 | 便于单测并减薄 composable |
| `frontend/src/lib/realtimeSessionEvents.js` | 抽出服务端事件处理 | 拆分 `useRealtimeChat` 中最大职责块 |
| `frontend/src/composables/useRealtimeChat.js` | 变薄为连接/录音/模式编排 | 保持 GuideView 对外 API 不变 |
| `frontend/tests/realtime-session-events.test.js` | 新增事件与消息态单测 | 覆盖拆分后的关键路径 |
| `frontend/src/lib/visitorCatalog.js` | 新增景点/美食列表请求 helper | Explore/Food/Map 去重 fetch |
| `frontend/src/features/explore/lib/attractionFilters.js` | 抽出景点筛选 | 与美食 `foodFilters` 对称 |
| `frontend/src/views/ExploreView.vue` | 接入 catalog + filters | 结构统一，行为不变 |
| `frontend/src/views/FoodView.vue` | 接入 catalog helper | 去掉页面内重复 fetch |
| `frontend/src/views/MapView.vue` | 接入 catalog，`allSettled` 并行加载 | 保留单侧失败时另一侧仍可用 |
| `frontend/tests/visitor-services.test.js` | 补充筛选与 catalog 测试 | 覆盖新共享模块 |
| `frontend/src/composables/useChat.js` | 删除 | SPA 内无引用的遗留 HTTP/SSE chat |
| `frontend/package.json` / `package-lock.json` | 移除未使用的 `echarts` 依赖 | 管理端继续用 `/static/vendor/echarts.min.js` |

Notes:
- `frontend`: `npm test` 55 passed；`npm run build` 成功。
- `pytest tests/test_frontend.py tests/test_admin_frontend.py`：因缺少 `TEST_DATABASE_URL` 全部 skipped。

## 09:37 游客端页面切换改为上滑

Task: 五个游客页路由切换特效改为向上滑动（当前页上滑离开，下一页自下方滑入）。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/src/route-transition.css` | `lake-dissolve` 改为 `page-slide-up` 纵向整页滑动 | 实现上滑切换 |
| `frontend/src/components/VisitorRouteTransition.vue` | Transition name 同步为 `page-slide-up` | 与新 CSS 类名匹配 |

## 10:06 修复页面滑动未生效

Task: 排查刷新后仍为淡入淡出；根因是后端提供的 `frontend/dist` 仍含旧 `lake-dissolve`。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/src/main.js` | 全局引入 `route-transition.css` | 保证过渡样式进入主包 |
| `frontend/src/components/VisitorRouteTransition.vue` | 去掉组件内 `<style src>` | 避免样式只挂在组件侧、与构建脱节 |
| `frontend/src/visitor-pages.css` | `visitor-content-shell` 改为 `overflow: hidden` | 让整页上滑在内容区裁剪可见 |
| `frontend/dist/**` | 重新 `npm run build` | 用 `page-slide-up` 替换旧淡入淡出产物 |

Notes:
- 构建产物已确认含 `page-slide-up` / `translateY(100%)`，不再含 `lake-dissolve`。
- 若经 FastAPI 访问 `/visitor`，需硬刷新；开发模式请用 Vite `npm run dev`。

## 13:01 互动地图精致化

Task: 自定义底图样式 + 精致标记/选中态 + 路线样式微调。

| File | Change | Reason |
| --- | --- | --- |
| `src/lingjing_ai/config/settings.py` | 增加 `map_js_style` / `MAP_JS_STYLE` | 可配置高德底图样式 |
| `src/lingjing_ai/api/app.py` | `MapConfigResponse` 返回 `map_style` | 前端建图读取样式 |
| `frontend/src/lib/mapVisuals.js` | 新增底图/标记/路线视觉 helper | 互动图与内嵌图共用 |
| `frontend/src/composables/useInteractiveMap.js` | 接入样式、选中态、路线端点 | 地图页精致化 |
| `frontend/src/composables/useRouteMap.js` | 同步底图与路线样式 | 聊天路线卡一致 |
| `frontend/src/map-places.css` | 升级标记与起终点样式 | 选中光晕与品牌色 |
| `frontend/src/views/MapView.vue` | `renderRoute` 传入 `routeMode` | 步行虚线/驾车实线 |
| `frontend/src/visitor-pages.css` | 画布内阴影微调 | 轻量景深 |
| `tests/test_amap_api.py` / `tests/test_settings.py` | 覆盖默认与自定义 map_style | 配置回归 |
| `frontend/tests/map-visuals.test.js` | 新增视觉 helper 单测 | 前端回归 |

Notes:
- `frontend`: `npm test` 59 passed；`npm run build` 成功。
- settings 相关 pytest 通过；amap API 用例因缺少 `TEST_DATABASE_URL` 被 skip。

## 13:06 提高地图对比度

Task: 底图与标记颜色过淡看不清，加深标记/路线并改用更高对比底图。

| File | Change | Reason |
| --- | --- | --- |
| `frontend/src/lib/mapVisuals.js` | 默认底图改为 `normal`，路线色加深加粗 | 提升可读性 |
| `frontend/src/map-places.css` | 标记加大、加深青绿/琥珀，描边加粗 | 标记更醒目 |
| `settings.py` / `app.py` / 相关测试 | 默认 `MAP_JS_STYLE` 同步为 `normal` | 与前端默认一致 |

## 14:10 恢复底图路线与画布外框

Task: 除标记样式和选中态外，底图、路线样式、画布外框恢复为精致化改造前版本。

| File | Change | Reason |
| --- | --- | --- |
| `useInteractiveMap.js` / `useRouteMap.js` | 去掉自定义 mapStyle/features 与路线端点美化 | 恢复默认底图与原路线 |
| `mapVisuals.js` | 仅保留地点标记 helper | 标记/选中态继续用 |
| `visitor-pages.css` | 画布外框恢复为朴素背景 | 按用户要求回退 |
| `MapView.vue` | 取消向 renderRoute 传 routeMode | 路线不再区分虚实线样式 |

