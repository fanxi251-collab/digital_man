# Daily Modify - 2026-07-22

## 11:09 PostgreSQL 数据校验与单一存储收口

Task: 备份遗留关系数据，精确补迁并逐表校验 PostgreSQL，清除旧存储路径与说明，完成 Docker 构建前的全项目审计。

| File | Change | Reason |
| --- | --- | --- |
| `src/lingjing_ai/storage/postgres.py` | 新增统一 schema 连接、事务、查询和建表辅助 | 四个业务 Store 共用安全的 PostgreSQL 访问层，减少重复连接逻辑 |
| `src/lingjing_ai/services/attraction_store.py`、`conversation_store.py`、`food_store.py`、`feedback_store.py` | 全部关系读写切换为 PostgreSQL；会话删除同时清理实时轮次标记 | 将 PostgreSQL 设为唯一关系事实源，并修复会话删除不完整的问题 |
| `src/lingjing_ai/config/settings.py`、`src/lingjing_ai/api/app.py` | 移除数据库连接默认值，缺少 `DATABASE_URL` 时快速失败 | 防止开发或容器环境静默连接硬编码数据库 |
| `tests/conftest.py` 及数据库集成测试模块 | 使用显式 fixture、随机 schema 和逐项清理 | 隔离测试数据，避免测试污染四个业务 schema |
| `tests/test_settings.py`、`tests/test_api.py`、`tests/test_conversation_store.py` | 增加必填连接配置和会话轮次清理回归测试 | 固化本次配置边界与一致性修复 |
| `pyproject.toml`、`requirements-docker.txt` | 声明 `psycopg[binary]` | 确保本地安装与 Docker 镜像都具备 PostgreSQL 驱动 |
| `scripts/refresh_attraction_seed_covers.py` | 通过项目设置连接 `attractions` schema | 运维脚本与应用使用同一数据源 |
| `.gitignore`、`src/lingjing_ai/realtime/session.py`、`src/lingjing_ai/assets/attractions/README.md` | 清除旧存储文件规则、注释和操作说明 | 工作区不再保留旧关系存储假设 |
| `project_structure.md`、`requirement.md`、`docs/conversation_design.md`、`docs/qwen_audio_realtime.md`、`docs/项目部署运行配置说明书.md` | 更新为 PostgreSQL-only 架构、备份恢复和容器配置说明 | 部署者必须注入外部 PostgreSQL，文件卷只保存图片、资料和向量数据 |
| `docs/superpowers/specs/2026-07-20-docker-competition-image-design.md` | 补充外部 PostgreSQL、镜像内容和失败策略 | 使比赛镜像设计与当前运行架构一致 |
| 历史 `daily-modify/` 记录及一次性迁移计划 | 改写旧存储表述并逐文件移除已完成计划 | 满足全仓不保留旧存储名称、文件后缀或说明的要求 |

Notes:
- 工作区外备份位于 `C:\tmp\lingjing-postgres-migration-20260722-100500`；源备份与迁移前 PostgreSQL dump 已校验并保留。
- 8 张业务表的行数、业务键集合和规范化整行 SHA-256 全部一致，最终输出 `EXACT_MIGRATION=true`。
- 后端测试分组验证：193 个纯测试通过，117 个 PostgreSQL 集成/API 测试通过，共 310 个；测试 schema 残留复查为 0。
- 前端测试 39 个通过，Vite 生产构建通过；Python `compileall` 和变更空白检查通过。
- Docker Buildx 成功构建 `lingjing-ai:postgres-audit`：`linux/amd64`、非 root 用户 `lingjing`、182322077 bytes；镜像 `/app` 中旧数据库文件与旧引用均为 0，缺少 `DATABASE_URL` 时按设计快速失败。
- 全局 Python 环境的 `pip check` 仍报告 `langchain-dashscope`、`streamlit` 的既有依赖冲突；Docker 隔离环境安装成功，本次未改动这些无关全局包。
- 测试产生的 `.pytest_tmp_*` 目录未批量删除，遵守项目禁止批量删除文件或目录的约束。

## 11:29 Pytest 测试产物集中整理

Task: 将项目根目录的 pytest 缓存与历史临时目录集中到统一测试产物目录，并配置后续测试默认路径。

| File | Change | Reason |
| --- | --- | --- |
| `test-artifacts/pytest/` | 新建 `cache/`、`current/`、`runs/` 结构，逐个移入 22 个现有目录 | 保留历史诊断内容，同时清理项目根目录的测试产物散落问题 |
| `pyproject.toml` | 设置 pytest `cache_dir` 和默认 `--basetemp` | 后续测试只在统一目录生成缓存和临时内容 |
| `.gitignore` | 忽略 `/test-artifacts/` | 避免运行产物进入 Git 状态或提交 |
| `docs/superpowers/specs/2026-07-22-pytest-artifact-organization-design.md` | 记录目录结构和安全边界 | 明确不删除、逐目录移动和工作区内路径约束 |
| `docs/superpowers/plans/2026-07-22-pytest-artifact-organization.md` | 记录逐项执行与验证步骤 | 让目录整理过程可审计和复查 |

Notes:
- 移动前活动 Python/pytest 进程为 0，22 个源与目标绝对路径均在当前工作区内。
- 22 个目录全部逐项移动成功，没有删除文件或目录；根目录 pytest 产物目录复查为 0。
- `python -m pytest --collect-only -q` 正常收集 310 项；设置单元测试 1 项通过。

## 13:06 宝塔 Docker Compose 部署包配置

Task: 为宝塔 Linux 面板准备 PostgreSQL 18、Neo4j 2026.05.0 Enterprise 和应用镜像的 Compose 部署文件，并离线导出当前知识图谱。

| File | Change | Reason |
| --- | --- | --- |
| `neo4j.dump` | 使用同版本 `neo4j-admin` 离线导出 `neo4j` 数据库 | 服务器必须恢复真实知识图谱，项目文档源已不足以完整重建当前图库 |
| `docker-compose.yml` | 定义应用、PostgreSQL、Neo4j 三服务、健康检查、持久卷和回环端口 | 宝塔只需加载镜像和 dump 即可按固定拓扑启动，数据库端口不会公开暴露 |
| `.env` | 改为 Compose 插值文件并生成独立服务器数据库密码 | 将数据库凭据集中在 Git 忽略文件中，避免写入 Compose 或应用镜像 |
| `app.env` | 迁移当前模型、地图和知识图谱配置，改用容器内 Neo4j 地址并关闭未部署的 Redis | 保持现有应用能力，同时避免容器错误访问宿主机回环地址或不存在的 Redis |
| `.gitignore` | 增加 `app.env` 和 `*.dump` 忽略规则 | 防止运行密钥和数据库归档被误提交 |
| `docs/superpowers/specs/2026-07-22-baota-compose-deployment-bundle-design.md` | 记录部署架构、文件职责、安全边界和恢复顺序 | 让宝塔部署方案和已确认的 Enterprise 许可选择可审计 |
| `docs/superpowers/plans/2026-07-22-baota-compose-deployment-bundle.md` | 记录逐项生成与验证步骤 | 让离线导出、密钥迁移和 Compose 校验过程可复现 |

Notes:
- 原 `.env` 已单文件备份到 `C:\tmp\lingjing-env-before-compose-20260722-125942.env`，源文件与备份 SHA-256 一致。
- `neo4j-admin database load --info` 成功识别归档：Neo4j ZSTD Dump、56 个文件、3979351 bytes。
- `docker compose config --quiet` 通过；镜像、卷、回环端口、数据库健康依赖和 PostgreSQL 18 卷路径均已核对。
- 环境文件语义校验通过：必需键齐全、容器内数据库主机正确、旧地图安全码已迁移、Compose 未包含真实密钥。
- 基线测试为后端 193 通过/117 跳过、前端 39 通过；配置与知识图谱聚焦测试 16 项通过。

## 17:19 互动地图本地配置恢复

Task: 修复游客端互动地图因本地服务未加载 `MAP_JS_SECURITY_CODE` 而不可用的问题。

| File | Change | Reason |
| --- | --- | --- |
| `.env` | 补入现有用户级 `MAP_JS_SECURITY_CODE`，未生成或输出新密钥 | 本地 FastAPI 直接启动时需要从工作区配置读取高德 JS API 安全码 |
| `daily-modify/2026-07-22.md` | 记录本次配置修复与验证结果 | 便于后续区分本地直启配置和 Docker `app.env` 配置 |

Notes:
- 重启本地 FastAPI 后，`/api/tools/map/config` 返回 `enabled=true`，且 JS Key 与安全码均已加载。
- 浏览器验收确认互动地图渲染高德底图、8 个景点与 6 处美食标记，页面和控制台均无地图配置错误。
- 本次未修改业务代码，也未输出或提交地图密钥。
