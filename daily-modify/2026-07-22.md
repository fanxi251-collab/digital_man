# Daily Modify - 2026-07-22

## 11:09 PostgreSQL 数据校验与单一存储收口

Task: 备份遗留关系数据，精确补迁并逐表校验 PostgreSQL，清除旧存储路径与说明，完成 Docker 构建前的全项目审计。

| File | Change | Reason |
| --- | --- | --- |
| `src/lingjing_ai/storage/postgres.py` | 新增统一 schema 连接、事务、查询和建表辅助 | 四个业务 Store 共用安全的 PostgreSQL 访问层，减少重复连接逻辑 |
| `src/lingjing_ai/services/attraction_store.py`、`conversation_store.py`、`food_store.py`、`feedback_store.py` | 全部关系读写切换为 PostgreSQL；会话删除同时清理实时轮次标记 | 将 PostgreSQL 设为唯一关系事实源，并修复会话删除不完整的问题 |
| `src/lingjing_ai/config/settings.py`、`src/lingjing_ai/api/app.py` | 移除数据库连接默认值，缺少 `DATABASE_URL` 时快速失败 | 防止开发或容器环境静默连接硬编码数据库 |
| `tests/conftest.py` 及数据库集成测试模块 | 使用显式 fixture、随机 schema 和逐项清理 | 隔离测试数据，避免测试污染四个业务 schema |
| `tests/test_settings.py`、`tests/test_api.py`、`tests/test_conversation_store.py` | 增加必填连接配置和会话轮次清理回归测试 | 固化本次配置边界与一致性修复 |
| `pyproject.toml`、`requirements-docker.txt` | 声明 `psycopg[binary]` | 确保本地安装与 Docker 镜像都具备 PostgreSQL 驱动 |
| `scripts/refresh_attraction_seed_covers.py` | 通过项目设置连接 `attractions` schema | 运维脚本与应用使用同一数据源 |
| `.gitignore`、`src/lingjing_ai/realtime/session.py`、`src/lingjing_ai/assets/attractions/README.md` | 清除旧存储文件规则、注释和操作说明 | 工作区不再保留旧关系存储假设 |
| `project_structure.md`、`requirement.md`、`docs/conversation_design.md`、`docs/qwen_audio_realtime.md`、`docs/项目部署运行配置说明书.md` | 更新为 PostgreSQL-only 架构、备份恢复和容器配置说明 | 部署者必须注入外部 PostgreSQL，文件卷只保存图片、资料和向量数据 |
| `docs/superpowers/specs/2026-07-20-docker-competition-image-design.md` | 补充外部 PostgreSQL、镜像内容和失败策略 | 使比赛镜像设计与当前运行架构一致 |
| 历史 `daily-modify/` 记录及一次性迁移计划 | 改写旧存储表述并逐文件移除已完成计划 | 满足全仓不保留旧存储名称、文件后缀或说明的要求 |

Notes:
- 工作区外备份位于 `C:\tmp\lingjing-postgres-migration-20260722-100500`；源备份与迁移前 PostgreSQL dump 已校验并保留。
- 8 张业务表的行数、业务键集合和规范化整行 SHA-256 全部一致，最终输出 `EXACT_MIGRATION=true`。
- 后端测试分组验证：193 个纯测试通过，117 个 PostgreSQL 集成/API 测试通过，共 310 个；测试 schema 残留复查为 0。
- 前端测试 39 个通过，Vite 生产构建通过；Python `compileall` 和变更空白检查通过。
- Docker Buildx 成功构建 `lingjing-ai:postgres-audit`：`linux/amd64`、非 root 用户 `lingjing`、182322077 bytes；镜像 `/app` 中旧数据库文件与旧引用均为 0，缺少 `DATABASE_URL` 时按设计快速失败。
- 全局 Python 环境的 `pip check` 仍报告 `langchain-dashscope`、`streamlit` 的既有依赖冲突；Docker 隔离环境安装成功，本次未改动这些无关全局包。
- 测试产生的 `.pytest_tmp_*` 目录未批量删除，遵守项目禁止批量删除文件或目录的约束。
